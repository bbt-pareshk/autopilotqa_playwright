name: Playwright Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      suite:
        description: "Test suite to run"
        required: true
        default: "smoke"
        type: choice
        options:
          - smoke
          - regression
      environment:
        description: "Target environment"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - live

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      # 1️⃣ Checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Node setup
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      # 3️⃣ Install deps
      - name: Install dependencies
        run: npm ci

      # 4️⃣ Install Playwright
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      # 5️⃣ Linux dependencies (safe list)
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 \
            libxkbcommon0 libxcomposite1 libxdamage1 libxrandr2 libgbm1 \
            libpango-1.0-0 libpangocairo-1.0-0 libgtk-3-0 \
            libxshmfence1 libx11-xcb1 libxcb-dri3-0 libxfixes3 \
            libxrender1 libxcb1 libxext6 fonts-liberation libwoff1 \
            wget || echo "Some packages failed, continuing..."

      # 6️⃣ Run tests
      - name: Run Playwright Tests
        env:
          ENVIRONMENT: ${{ inputs.environment }}
          CI: true
        run: |
          echo "Running ${{ inputs.suite }} suite on ${{ inputs.environment }}"
          if [ "${{ inputs.suite }}" = "smoke" ]; then
            npx playwright test --grep-tag @smoke || true
          else
            npx playwright test --grep-tag @regression || true
          fi

      # 7️⃣ Optional severity gate
      - name: Fail build on critical failures
        run: |
          if [ -f scripts/check-critical.js ]; then
            node scripts/check-critical.js || true
          else
            echo "No critical check script found"
          fi

      # 8️⃣ Generate Allure
      - name: Generate Allure Report
        run: |
          npm install -g allure-commandline
          mkdir -p allure-report
          allure generate allure-results --clean -o allure-report || true

      # 9️⃣ Upload Allure artifact
      - name: Upload Allure Report Artifact
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report

      # 🔟 Upload Playwright HTML report
      - name: Upload Playwright HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: playwright-html-report
          path: playwright-report

      # 1️⃣1️⃣ Deploy Allure to GitHub Pages
      - name: Deploy Allure Report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./allure-report
          publish_branch: gh-pages
          user_name: github-actions
          user_email: github-actions@github.com

      # 1️⃣2️⃣ CI Summary links
      - name: Add CI Summary Links
        run: |
          echo "## 📊 Test Reports" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔵 Allure Report (Online)" >> $GITHUB_STEP_SUMMARY
          echo "👉 https://${GITHUB_REPOSITORY_OWNER}.github.io/${GITHUB_REPOSITORY#*/}/" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🟢 Playwright HTML Report" >> $GITHUB_STEP_SUMMARY
          echo "👉 Download from **Artifacts → playwright-html-report**" >> $GITHUB_STEP_SUMMARY
